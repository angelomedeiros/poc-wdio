image: node:14.4

hello world:
  services:
    - selenium/standalone-chrome
  script:
    - uname -a
    - lsb_release -a
    - docker
    - docker ps
    - npm i
    - npm run test -- --host=selenium__standalone-chrome
    - ls -ltra
