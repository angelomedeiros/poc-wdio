image: node:14.4

hello world:
  services:
    - name: selenium/standalone-firefox
  variables:
    HOSTNAME_DRIVER: selenium__standalone-firefox
    PORT_DRIVER: 4444
  script:
    - yarn
    - yarn test
    - ls -ltra
