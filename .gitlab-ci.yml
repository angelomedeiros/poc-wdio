image: node:14.4

hello world:
  services:
    - name: selenium/standalone-firefox
  script:
    - curl selenium__standalone-firefox:4444
    - yarn
    - yarn test --hostname=selenium
    - ls -ltra
